import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    box: {
        width: '100%',
        flex: 1,
    },
    _web: { overflow: 'hidden' },
    _light: { backgroundColor: 'colorGray.50' },
    _dark: { backgroundColor: 'colorGray.900' },
})
